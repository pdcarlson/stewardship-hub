{
  "name": "approve-verification-request",
  "version": "1.0.0",
  "description": "Appwrite function to approve a user's verification request.",
  "main": "src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-appwrite": "^11.0.0" 
  }
}